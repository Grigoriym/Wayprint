package com.grappim.wayprint.uikit.theme

import androidx.compose.material3.Typography

/**
 * Material 3's default type scale, untouched, default platform font. This is the single edit
 * point for the day the app ships a font of its own — `MaterialTheme` is handed this and never
 * the default.
 */
val WayprintTypography = Typography()
